## What

Brief description of changes.

## Why

Why are these changes needed?

## Checklist

- [ ] `npm run check` passes
- [ ] `cargo test` in `src-tauri` passes; parser changes come with a fixture-based test
- [ ] Nothing new is written to `~/.claude` (Skillet stays read-only there)
- [ ] Updated `CHANGELOG.md` under "Unreleased"
- [ ] Updated `README.md` if user-facing changes
